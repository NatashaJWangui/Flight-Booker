x<!DOCTYPE html>
<html>
  <head>
    <title>Flight Booker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Navigation -->
    <% unless controller_name == 'flights' && action_name == 'index' %>
      <nav class="navbar">
        <%= link_to "🛫 NJW Airlines", root_path, class: "brand" %>
      </nav>
    <% end %>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="flash-message flash-notice">
        <%= notice %>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="flash-message flash-alert">
        <%= alert %>
      </div>
    <% end %>

    <!-- Main Content -->
    <%= yield %>
  </body>
</html>